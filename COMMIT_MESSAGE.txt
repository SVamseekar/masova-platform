feat: Clean database and remove all hardcoded store IDs

BREAKING CHANGES:
- Database cleaned: removed all stores, users, orders, and test data
- Only menu items (116) and recipes (9) retained
- Header-based store filtering PARTIALLY implemented (INCOMPLETE)

Backend Changes:
- Remove hardcoded STORE001 from inventory-service PurchaseOrderService
- Remove hardcoded STORE001 from analytics-service (BenchmarkingService, ExecutiveReportingService)
- Add getAllStoreIds() method to InventoryService for dynamic store fetching
- Fix notification-service build by adding spring-boot-starter-security dependency

Frontend Changes:
- Remove hardcoded "store-1" from KitchenDisplayPage (use Redux store selector)
- Remove hardcoded "store-1" from PaymentPage (2 locations)
- Update StoreManagementPage placeholder from "store-1" to "DOM001"
- Fix neumorphic theme error in InventoryDashboardPage ('pressed' → 'inset')
- START header-based filtering in inventoryApi.ts (INCOMPLETE - see SESSION_SUMMARY.md)

Database:
- Run scripts/cleanup-database.js to clear all data except menu
- Delete all hardcoded seed scripts (seed-store.js, user registration scripts)

Files Deleted:
- scripts/seed-store.js
- scripts/register-users-clean.ps1
- scripts/temp-users.ps1
- scripts/create-demo-users.ps1

Files Created:
- scripts/cleanup-database.js
- SESSION_SUMMARY.md
- COMMIT_MESSAGE.txt

⚠️ WARNING: App is currently BROKEN
Header-based store filtering is incomplete. Next session MUST:
1. Update ALL frontend API files to send X-User-Id, X-User-Type, X-User-Store-Id, X-Selected-Store-Id headers
2. Remove storeId query parameters from ~100+ frontend API endpoints
3. Update ALL backend controllers to extract storeId from headers instead of query params
4. Test complete flow

See SESSION_SUMMARY.md for complete details and next steps.

Co-Authored-By: Claude <noreply@anthropic.com>
